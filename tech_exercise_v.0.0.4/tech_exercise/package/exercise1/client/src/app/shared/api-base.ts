import { environment } from '../../environments/environment';

/** API base URL for HTTP requests. From environment (e.g. http://localhost:5204 in dev, '' in prod). */
export const apiBase = environment.apiUrl;
